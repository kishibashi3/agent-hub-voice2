# voice-gateway v2 依存パッケージ

# Pipecat + LiveKit transport + Gemini Live + Silero VAD
pipecat-ai[livekit,google,silero]>=1.3.0

# LiveKit API (token 生成)
livekit-api>=0.7.0

# agent-hub SDK (PyPI 未公開 — GitHub 直 URL でインストール)
agent-hub-sdk @ git+https://github.com/kishibashi3/agent-hub-sdk.git@v0.7.0#subdirectory=python

# HTTP サーバー
aiohttp>=3.9.0

# Google Generative AI (Gemini tools schema)
google-generativeai>=0.8.0
